#include "main.h"

/**
 * add - adds two integers and returns the result
 * @d: int to be added to u
 * @u: int to be added to d
 * Return: sum of d and u
 */
int add(int d, int u)
{
	int c;

	c = d + u;

	return (c);
}
